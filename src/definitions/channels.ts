import { messages } from './messages'

export const channels = {
  channel: {
    messages,
    message: {
      tags: {
        id: {}
      }
    },
    conversation: {
      tags: {
        id: {}
      }
    }
  }
}